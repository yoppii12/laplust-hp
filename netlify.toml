# Netlify ビルド設定（要件定義書5章：Netlify先行導入）
[build]
  base = "site"
  command = "npm run build"
  publish = "out"

# Netlify Functions（問い合わせフォーム受信用。要件定義書8-3節）
# 実装は site/netlify/functions/ に配置する
[functions]
  directory = "netlify/functions"
